[project]
name = "agentic-systems-showcase"
version = "1.0.0"
description = "Inspectable reference patterns for bounded AI agents"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

